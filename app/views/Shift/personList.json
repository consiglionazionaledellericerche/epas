[
  	#{list personList, as:'person'}
  		{
  			"id" : ${person.id},
  			"email" : "${person.contactData.email}",
  			"name" : "${person.name}",
  			"surname" : "${person.surname}",
  			"mobile": "${person.contactData.mobile}",
  			"jolly": ${person.personShift.jolly}
  		}${person_isLast ? '' : ','}	
  	#{/list}
]