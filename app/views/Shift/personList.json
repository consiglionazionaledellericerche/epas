[
  	#{list personList, as:'person'}
  		{
  			"id" : ${person.id},
  			"email" : "${person.email}",
  			"name" : "${person.name}",
  			"surname" : "${person.surname}",
  			"mobile": "${person.mobile}",
  			"jolly": ${person.personShift.jolly}
  		}${person_isLast ? '' : ','}	
  	#{/list}
]