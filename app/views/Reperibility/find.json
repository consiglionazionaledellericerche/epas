[
  %{ 
  	for (reperibilityPeriod in reperibilityPeriods) {
  		%}
  			{
  			"id" : "${reperibilityPeriod.person.id}",
  			"start" : ${reperibilityPeriod.start},
  			"end" : ${reperibilityPeriod.end}
  			},
  		%{
  	}
  }% 
]
