#####################################################################
# Questo file contiene le informazioni da impostare per lo sviluppo #
#####################################################################

# Url del db postgres

%dev.db=postgres://epas:orario@/localhost/epas-devel

%dev.shib.login=false

# If you need to change the HTTP port, uncomment this (default is set to 9000)
# http.port=9001

# disabilita i job
# %dev.jobs.active=false

# Abilita le timbrature via web da qualsiasi ip
%dev.skip.ip.check=true

#
#	Proprietà per l'autenticazione delle chiamate rest

%dev.rest.protime.user=protime
%dev.rest.protime.password=protime

%dev.perseo.base=http://localhost:9001
%dev.perseo.rest.people=/rest/people/
%dev.perseo.rest.departments=/rest/departments/

# Rest address and information
%dev.people.rest =http://localhost:9001/rest/people/listbydepartment?departmentCode=
%dev.people.rest.user =app.epas
%dev.people.rest.password =trapocolapuoicambiare
%dev.perseo.department=http://localhost:9001/rest/departments/getdepartmentbycodeid?codeId=

### Impostazioni server smtp

# mail.smtp.from Viene usato automaticamente dal mailer Play per popolare il campo From e ReplyTo nelle mail
%dev.mail.smtp.from=epas@iit.cnr.it

#%dev.mail.smtp.host=
#%dev.mail.smtp.user=
#%dev.mail.smtp.pass=
#%dev.mail.smtp.port=       25 when mail.smtp.channel is set to clear or starttls; 465 when mail.smtp.channel is set to ssl
#%dev.mail.smtp.channel=    clear,ssl,starttls      Default: clear
#%dev.mail.smtp.protocol=   smtp,smtps              Default: smtp
